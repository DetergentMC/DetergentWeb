# DetergentWeb

DetergentMC の公式サイト。ビルド不要の静的サイト (`index.html` / `style.css`)。
`main` への push で GitHub Pages に自動デプロイされる (`.github/workflows/pages.yml`)。

ローカル確認:

```bash
python -m http.server 8080
```
